- hosts: localhost
  tasks:
    - name: Stop old container
      command: docker stop feedback-app
      ignore_errors: yes

    - name: Remove old container
      command: docker rm feedback-app
      ignore_errors: yes

    - name: Run new container
      command: docker run -d -p 5000:5000 --name feedback-app feedback-app